workflows:
  ios-app-store:
    name: Ahmed iOS — App Store
    instance_type: mac_mini_m2
    max_build_duration: 120
    triggering:
      events:
        - push
      branch_patterns:
        - pattern: main
          include: true
          source: true
    integrations:
      app_store_connect: Ahmed
    environment:
      ios_signing:
        distribution_type: app_store
        bundle_identifier: com.darraha.ahmed
      vars:
        BUNDLE_ID: com.darraha.ahmed
        XCODE_WORKSPACE: ios/App/App.xcworkspace
        XCODE_SCHEME: App
        APP_STORE_APPLE_ID: com.darraha.ahmed
        APP_STORE_CONNECT_ISSUER_ID: c46c0b74-7d00-42b2-9786-333b76dacf91
        APP_STORE_CONNECT_KEY_IDENTIFIER: R46D76UXCH
      xcode: latest
      node: 22
      cocoapods: default
    scripts:
      - name: Install JS
        script: |
          npm ci
          npx prisma generate
      - name: Register bundle ID on Apple
        script: |
          app-store-connect bundle-ids create com.darraha.ahmed \
            --name "Ahmed" \
            --platform IOS || true
      - name: Native web + Capacitor iOS
        script: |
          npm run build:native
          cd ios/App
          pod install
      - name: Set up code signing
        script: |
          xcode-project use-profiles \
            --custom-export-options '{"testFlightInternalTestingOnly": true}'
      - name: Increment build number
        script: |
          cd "$CM_BUILD_DIR/ios/App"
          BUILD_NUMBER="${CM_BUILD_NUMBER:-$(date +%Y%m%d%H%M)}"
          agvtool new-version -all "$BUILD_NUMBER" || true
      - name: Build ipa
        script: |
          xcode-project build-ipa \
            --workspace "$CM_BUILD_DIR/$XCODE_WORKSPACE" \
            --scheme "$XCODE_SCHEME"
    artifacts:
      - build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log
    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: true
        copyright: 2026 Ahmed Al-Saadi

  android-internal:
    name: Ahmed Android
    instance_type: linux
    max_build_duration: 60
    environment:
      java: 17
      node: 22
    scripts:
      - name: Install
        script: |
          npm ci
          npx prisma generate
          npm run build:native
      - name: Assemble debug APK
        script: |
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug
    artifacts:
      - android/app/build/outputs/apk/debug/*.apk
